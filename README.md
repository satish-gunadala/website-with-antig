# DevOps & Cloud Training Website

A modern, responsive educational website built with React, Vite, and Tailwind CSS.

## 🚀 Getting Started

### Prerequisites

*   [Node.js](https://nodejs.org/) (Version 16 or higher)
*   npm (comes with Node.js)

### Installation

1.  **Clone the repository** (if applicable) or navigate to the project directory:
    ```bash
    cd devops-training-site
    ```

2.  **Install dependencies**:
    ```bash
    npm install
    # or
    yarn install
    ```

3.  **Run the development server**:
    ```bash
    npm run dev
    # or
    yarn dev
    ```

4.  **Open in your browser**:
    Access the application at `http://localhost:5173`

## 🛠 Tech Stack

*   **Frontend**: React + Vite
*   **Styling**: Tailwind CSS
*   **Icons**: Lucide React
*   **Animations**: Framer Motion
*   **Routing**: React Router DOM

## 📂 Project Structure

*   `src/components/layout`: Navbar, Footer
*   `src/components/ui`: Reusable UI cards (VideoCard, CourseCard, etc.)
*   `src/data`: JSON files serving as the database
*   `src/pages`: Main application views

## 📝 Customization

To add your own content, simply edit the JSON files located in the `src/data` directory. No code changes required!

---
Built with ❤️ for DevOps Engineers
